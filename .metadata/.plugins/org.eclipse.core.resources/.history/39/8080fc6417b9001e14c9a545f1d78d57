package hello.hellospring;

import javax.sql.DataSource;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import hello.hellospring.repository.JdbcMemberTemplateMemberRepository;
import hello.hellospring.repository.JpamemberRepository;
import hello.hellospring.repository.MemberRepository;
import hello.hellospring.service.MemberService;
import jakarta.persistence.EntityManager;

@Configuration
public class SpringConfig {
	
	private EntityManager em;
	
	@Autowired
	public SpringConfig(EntityManager em) {
		this.em = em;
	}
	
	
	@Bean
	MemberService memberService() {
		return new MemberService(memberRepository());
	}

	
	@Bean
	MemberRepository memberRepository() {
//		 MemoryMemberRepository(); // DB에 sql 던지는 방식
//		return new JdbcMemberRepository(dataSource); // Try Catch 로 연결하는방식
//		return new JdbcMemberTemplateMemberRepository(dataSource); //
		return new JpamemberRepository(em);
	}

}
